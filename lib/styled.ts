import * as styledComponents from 'styled-components';
import { ThemedStyledCompoenntsModule } from 'styled-components'; 


const {
	default: styled,
	css,
	createGlobalStyle,
	keyframes,
} = styledComponents as ThemedStyled