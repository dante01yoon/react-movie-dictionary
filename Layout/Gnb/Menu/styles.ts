import styled, { css } from 'styled-components';

const StyledMenuList = styled.div`
`;

const StyledMenuItem = styled.li`
`;

const StyledDropdownUnderlay = styled.div`
`; 

const StyledMenuDropdown = styled.ul`
`;
const StyledMenuDropdownItem = styled.li`
`;
const Styled = {
  StyledMenuList, 
  StyledMenuItem,
  StyledDropdownUnderlay,
  StyledMenuDropdown,
  StyledMenuDropdownItem
};

export default Styled; 
